
{{#each model as |edition|}}
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">{{edition.name}}</h3>
    </div>
    <div class="panel-body">
      <p>Date: {{edition.date}}</p>
    </div>
  </div>
{{/each}}