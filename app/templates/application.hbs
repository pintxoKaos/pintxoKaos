<img class="rain" id="background" src="assets/images/bg4.jpg" />

<nav class="navbar navbar-default" {{action 'quellueva'}}>

	<div class="container-fluid">

		<div class="navbar-header">

			<button {{action 'toggleMenu'}} type="button"
						class="navbar-toggle" data-toggle="collapse"
						data-target="#navbar" aria-controls="navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
<!--
	    {{#link-to 'index' class="navbar-brand"}}
	    	<span class="glyphicon glyphicon-home" aria-hidden="true"></span>
	    {{/link-to}}
-->
			<ul class="nav navbar-nav language">
				<li class="dropdown {{if dropdownOpen 'open'}}">
					<a href="#" class="dropdown-toggle navbar-brand" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" {{action 'toggleDropdown'}}><img src="{{t 'localeFlag'}}" class="icon-flag carret"></a>
					<ul class="dropdown-menu">
{{#each languages as |language|}}
<li>
	<a href="#" {{action 'changeLocale' language.code}} name="{{language.code}}"><img src="{{language.flagUrl}}" class="icon-flag"> {{language.name}}</a>
</li>
{{/each}}
					</ul>
				</li>
			</ul>


		</div>


	    <div class="collapse navbar-collapse {{if isMenuOpened 'in'}}" id="navbar">

			<ul class="nav navbar-nav">
				<li {{action 'toggleMenuToClose'}} title="{{t 'appName'}}">
				{{#link-to 'index'}}
					<span class="glyphicon glyphicon-home" aria-hidden="true"></span>
				{{/link-to}}
				</li>
				<li {{action 'toggleMenuToClose'}} title="Programa del concurso">
				{{#link-to 'program'}}
					<span class="glyphicon glyphicon-th-list" aria-hidden="true"></span>
				{{/link-to}}
				</li>
				<li {{action 'toggleMenuToClose'}} title="Orden de los participantes">
				{{#link-to 'orden'}}
					<span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span>
				{{/link-to}}
				</li>
				<li {{action 'toggleMenuToClose'}} title="Bases del concurso">
				{{#link-to 'bases'}}
					<span class="glyphicon glyphicon-paperclip" aria-hidden="true"></span>
				{{/link-to}}
				</li>						
				<li {{action 'toggleMenuToClose'}} title="Imprime una papeleta">
				{{#link-to 'votaPapel'}}
					<span class="glyphicon glyphicon-print" aria-hidden="true"></span>
				{{/link-to}}
				</li>						
{{#if session.isAuthenticated}}
				<li {{action 'toggleMenuToClose'}} title="VOTA!!!">>
				{{#link-to 'vota'}}
					<span class="glyphicon glyphicon-thumbs-up" aria-hidden="true"></span>
				{{/link-to}}
				</li>
				<li {{action 'toggleMenuToClose'}} title="Escrutinio">>
				{{#link-to 'scrutiny'}}
					<span class="glyphicon glyphicon-blackboard" aria-hidden="true"></span>
				{{/link-to}}
				</li>				<!--li>
				{{#link-to 'editions'}}
					<span class="glyphicon glyphicon-calendar" aria-hidden="true"></span>
				{{/link-to}}
				</li>
				<li>
				{{#link-to 'teams'}}
					<span class="glyphicon glyphicon-user" aria-hidden="true"></span>
					<span class="glyphicon glyphicon-flag" aria-hidden="true"></span>
				{{/link-to}}
				</li>
				<li>
				{{#link-to 'pintxos'}}
					<span class="glyphicon glyphicon-cutlery" aria-hidden="true"></span>
				{{/link-to}}
				</li-->
{{/if}}
			</ul>

			<ul class="nav navbar-nav navbar-right">
{{#if session.isAuthenticated}}
				<li {{action 'toggleMenuToClose'}} title="Tus datos">
					{{#link-to 'welcome'}}
{{#if session.currentUser.photoURL}}
						<img class="avatar" src='{{session.currentUser.photoURL}}' />
{{/if}}
{{#if session.currentUser.displayName}}
						{{session.currentUser.displayName}} 
{{else}}
						{{session.currentUser.email}} 
{{/if}}

				    {{/link-to}}
				</li>
{{else}}
				<li {{action 'toggleMenuToClose'}} title="Identifícate">
					{{#link-to 'sign-in'}}
						<span class="glyphicon glyphicon-log-in" aria-hidden="true"></span>
					{{/link-to}}
				</li>
{{/if}}

			</ul>

	    </div>

	</div>

</nav>


<div class='container'>
    {{outlet}}
</div>
