
<nav class="navbar navbar-default">

	<div class="container-fluid">

		<div class="navbar-header">

			<button {{action 'toggleMenu'}} type="button"
						class="navbar-toggle" data-toggle="collapse"
						data-target="#navbar" aria-controls="navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>

	    {{#link-to 'index' class="navbar-brand"}}
	    	<span class="glyphicon glyphicon-home" aria-hidden="true"></span>
	    {{/link-to}}

		</div>

    <div class="collapse navbar-collapse {{if isMenuOpened 'in'}}" id="navbar">

			<ul class="nav navbar-nav">
<!--
				{{#each menuLinks as |link|}}

					<li>
						{{link-to (t link.label) link.url}}
					</li>

				{{/each}}
-->
        <li>
        	<a target="_blank" href="http://moldingfest.com/">blog</a>
        </li>

			</ul>

			<ul class="nav navbar-nav navbar-right">
		        {{#if session.isAuthenticated}}
				    <li>{{#link-to 'welcome'}}
{{#if session.currentUser.displayName}}
 {{session.currentUser.displayName}}
{{else}}
 {{session.currentUser.email}}
{{/if}}
{{#if session.currentUser.photoURL}}
<img class="avatar" src='{{session.currentUser.photoURL}}' />
{{/if}}
				    {{/link-to}}</li>

<li><a {{action 'signOut'}}><span class="glyphicon glyphicon-log-out" aria-hidden="true"> </span></a></li>
		         {{else}}
				    <li>{{#link-to 'sign-in'}}
				        <span class="glyphicon glyphicon-log-in" aria-hidden="true"></span>
				    {{/link-to}}</li>
				    <li>{{#link-to 'sign-up'}}
				        <span class="glyphicon glyphicon-off" aria-hidden="true"></span>
				    {{/link-to}}</li>
		        {{/if}}




<!--
				<li>{{#link-to 'admin'}}<span class="glyphicon glyphicon-dashboard" aria-hidden="true"></span>{{/link-to}}</li>

				{{#if session.isAuthenticated}}
					<li>{{#link-to 'admin'}}<span class="glyphicon glyphicon-dashboard" aria-hidden="true"></span>{{/link-to}}</li>
				{{/if}}

				<li class="dropdown {{if dropdownOpen 'open'}}">
					<a href="#" class="dropdown-toggle navbar-brand" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" {{action 'toggleDropdown'}}><img src="{{t 'localeFlag'}}" class="icon-flag carret"></a>
					<ul class="dropdown-menu">
				{{#each languages as |language|}}
<li><a href="#" {{action 'changeLocale' language.code}} name="{{language.code}}"><img src="{{language.flagUrl}}" class="icon-flag"> {{language.name}}</a></li>
				{{/each}}
					</ul>
				</li>
-->
			</ul>

	    </div>
	</div>
</nav>

<div class='container'>  
    {{outlet}}
</div>
