


{{#if session.isAuthenticated}}
	<div class='well'>
    <h4>


      {{#if session.currentUser.displayName}}
        {{session.currentUser.displayName}} 
      {{else}}
        {{session.currentUser.email}} 
      {{/if}}


      {{#if haVotado}}
      ha votado satisfactoriamente. Ver 
              {{#link-to 'scrutiny'}}
      <button class="btn btn-danger active" type="button">
                <span class="glyphicon glyphicon-blackboard" aria-hidden="true"><span class="glyphicon glyphicon-stats" aria-hidden="true"><span class="glyphicon glyphicon-thumbs-up" aria-hidden="true"></span></span></span>
      </button>
              {{/link-to}}
      los resultados
      {{else}}
      todavía no ha votado
              {{#link-to 'vota'}}
      <button class="btn btn-danger active" type="button">
                <span class="glyphicon glyphicon-thumbs-up" aria-hidden="true"></span>
      </button>
              {{/link-to}}
      {{/if}}

    </h4>

    <div class="progress">
      <div class="progress-bar progress-bar-info" role="progressbar"
        aria-valuenow="{{votos.length}}" aria-valuemin="0" aria-valuemax="{{users.length}}"
        style="width: {{porcentaje}}%;">
        <span class="label-success">{{porcentaje}}% {{votos.length}} / {{users.length}}</span>
      </div>
      <span class="label-danger">{{porcentaje}}% {{votos.length}} / {{users.length}}</span>
    </div>

  </div>
{{else}}
	<!--section class="content wrapper flexy-item">
		<header class="accesible-hide">
			<h2>{{t 'appName'}}</h2>
		</header>
	</section-->

	<div class='well'>
	<div class='jumbotron'>
  <h6>Fundido en negro......</h6>
  <h5>Cuerdas en <i>sotto voce</i>......</h5>
  <h6>Murmullo de gente de fondo......</h6>
  <h4>Violines <i>crescendo</i>......</h4>
  <h4>Sonido de útiles de cocina......</h4>
  <h4>Entran las trompas......</h4>
  <h3>Las maderas atacan la nota en <i>sforzando</i>......</h3>
  <h5>se escucha: </h5>
  <h2>”Tenemos que emplatar ya, ¡tenemos que emplatar ya!”......</h2>
  <h2>ENTRAN TODOS LOS METALES EN UN GRAN ACORDE..................</h2>
  <h5>y se lee:</h5>
  <h1><span class="label-success">PINTXOKAOS</span></h1><h1><span class="label-danger">EPISODE IV</span></h1>
	</div>
  <p>{{t 'hiAreIn'}} {{t 'appName'}}</p>
	<p>En esta edición hay que {{#link-to 'sign-up' class='btn btn-default'}}registrarse <span class="glyphicon glyphicon-off" aria-hidden="true"></span>{{/link-to}} con un correo electrónico y una clave. </p>
	<p>{{t 'register3'}}. </p>
	<p>Si ya estás registrado necesitarás {{#link-to 'sign-in' class='btn btn-default'}}logarte <span class="glyphicon glyphicon-log-in" aria-hidden="true"></span>{{/link-to}} para poder votar, elegir un equipo, un pintxo... etc. </p>
	<p>{{t 'siCond'}} {{moment-from-now (now) interval=1000}} {{t 'endCond'}}. {{t 'askHelp'}}.</p>
	<p>{{t 'gracias'}}</p>
	</div>
{{/if}}





<!--
	<div class='well'>
{{#if editions.length}}



{{/if}}
<button class="btn btn-success" type="button">
  Ediciones <span class="badge">{{editions.length}}</span>
</button>
<button class="btn btn-success" type="button">
  Votos <span class="badge">{{votos.length}}</span>
</button>
<button class="btn btn-success" type="button">
  Equipos <span class="badge">{{teams.length}}</span>
</button>
<button class="btn btn-danger active" type="button">
  Participantes <span class="badge">{{users.length}}</span>
</button>



 {{#each (each_with_index) editions as |edition|}}
  <li>{{index}} {{edition.name}}</li>
 {{/each}}

<p>{{moment-duration nextEdition}}</p>
<p>{{moment-format nextEdition}}</p>
<p>is before {{is-before nextEdition comparison precision='second'}}</p>
<p>is after {{is-after nextEdition comparison precision='second'}}</p>

{{#if session.isAuthenticated}}


	<p>{{t 'chooseTeam'}} {{#link-to 'teams' class='btn btn-default'}}<span class="glyphicon glyphicon-user" aria-hidden="true"></span><span class="glyphicon glyphicon-flag" aria-hidden="true"></span>{{/link-to}}</p>
	<p>{{t 'choosePintxo'}} {{#link-to 'pintxos' class='btn btn-default'}}<span class="glyphicon glyphicon-cutlery" aria-hidden="true"></span>{{/link-to}}</p>
	
 {{else}}

{{/if}}
	</div>
	-->

  {{outlet}}