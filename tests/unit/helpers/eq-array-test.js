
import { eqArray } from 'mf/helpers/eq-array';
import { module, test } from 'qunit';

module('Unit | Helper | eq array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eqArray([42]);
  assert.ok(result);
});

